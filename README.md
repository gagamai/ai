# ai

Thermo-modal parametric simulation workspace for non-uniform thermal environments.

This repository records major iterations from `C:\Users\Lenovo\Desktop\参数化`, including MATLAB drivers, APDL templates, generated configurations, six-temperature results, and iteration notes.

## Current recommendation

See `current/` for the accepted simulation summary, config, deterministic six-temperature design table, and result CSV.

Accepted version: `iterations/iter13_final_paper_uniform6_four_edge_support`.

Key result: the paper-like non-uniform thermal field plus virtual-boundary model gives front-four-mode six-point frequency variations of `49.05%`, `60.35%`, `54.55%`, and `45.79%`, satisfying the `>=35%` target without zero or negative frequencies.
